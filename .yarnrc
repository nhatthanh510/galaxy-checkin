# A transitive dev dep (eslint-visitor-keys) declares an overly narrow Node
# engine range that excludes Node 23. It runs fine there, so we don't let the
# engine check block installs.
--ignore-engines true
